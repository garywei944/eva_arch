#!/bin/bash

# [[ $(pgrep compton) ]] || compton -b --backend glx --vsync opengl-swc
# [[ $(pgrep chrome) ]] || google-chrome --password-store=gnome --no-startup-window &
# [[ $(pgrep albert) ]] || albert &
numlockx on
# waw
