# eva_arch project context

This repository stores Gary's home-directory dotfiles with `/home/aris` as the Git worktree.

- Make targeted changes; preserve unrelated user files and the existing dotfile/symlink layout.
- Do not run the destructive bootstrap commands from `README.md` unless Gary explicitly requests a reinstall.
- Never use blanket clean/reset operations against the home worktree.
- Validate the affected configuration and report the resulting Git diff and uncommitted state.
- Global EVA persona, memory, and skills are runtime context, not project rules.
